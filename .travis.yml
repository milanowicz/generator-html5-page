language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-html5' ]; then cd .. && eval "mv $currentfolder generator-html5" && cd generator-html5; fi

